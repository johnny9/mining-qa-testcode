[runner]
artifacts_dir = "../artifacts"
tests_dir = "../tests/e2e"
pattern = "test*.py"
verbosity = 2
log_level = "INFO"
cleanup_timeout = 120
# Enable only tests associated with target PR firmware. Empty by default.
validation_prs = []

[publishers.local]
enabled = true
required = true
filename = "report.html"
json_filename = "result.json"

[[devices]]
name = "bitaxe-602-lab-1"
type = "bitaxe_602"
enabled = true

[devices.options]
read_only = false
publication_name = "Bitaxe Gamma 602"

[devices.interfaces.api]
base_url = "http://gamma-02.local"
timeout = 5
retries = 2
retry_backoff = 0.5
poll_interval = 0.5
online_timeout = 120

[devices.interfaces.websocket]
enabled = true
required = false

[devices.interfaces.serial]
# Set false (or omit this table) for network-only testing and OTA.
enabled = true
port = "/dev/serial/by-id/usb-Espressif_USB_JTAG_serial_debug_unit_*-if00"
baudrate = 115200
capture = true
required = true

[devices.interfaces.upgrade]
enabled = false
method = "ota"
expected_version = "replace-with-target-version"
application = "../../ESP-Miner/build/esp-miner.bin"
web = "../../ESP-Miner/build/www.bin"

[tests.public_pool_smoke]
host = "public-pool.io"
port = 3333
username_env = "MINER_TEST_POOL_USER"
password_env = "MINER_TEST_POOL_PASSWORD"
configure_device = true
suggested_difficulty = 1000
min_hashrate_ghs = 100
max_work_age_seconds = 60
readiness_timeout = 120
stable_samples = 10
minimum_job_notifications = 1
probe_timeout = 30

[tests.stratum_v1_regression]
# Address of this test host as seen from the miner. The server binds all local
# interfaces below and chooses an unused port when port=0.
advertised_host = "192.168.1.10"
bind_host = "0.0.0.0"
port = 0
username = "stratum-regression.worker"
# Explicitly opt in only on a trusted LAN/test host. The fake server drops and
# redacts the existing write-only pool password as soon as authorize is parsed.
allow_existing_device_password = false
# Safer alternative: set both this and devices.options.baseline_stratum_password_env.
# temporary_password_env = "MINER_TEST_FAKE_STRATUM_PASSWORD"
share_difficulty = 256
changed_difficulty = 512
handshake_timeout = 45
share_timeout = 45
reconnect_timeout = 45
accept_timeout = 20

[tests.stratum_v2_regression]
# Use the test host's LAN address. The fake listener generates a fresh
# authority key and certificate for every run, then configures that public key
# on the miner for authenticated Noise transport.
advertised_host = "192.168.1.10"
bind_host = "0.0.0.0"
port = 0
username = "stratum-v2-regression.worker"
channel_type = "extended" # "standard" or "extended"
share_difficulty = 256
changed_difficulty = 512
handshake_timeout = 45
share_timeout = 45
reconnect_timeout = 45
accept_timeout = 20
